/**
 * LINE リッチメニュー定義
 *
 * リッチメニューの構成（2行 × 3列）:
 *
 *  ┌──────────────┬──────────────┬──────────────┐
 *  │  AI問診      │ AI姿勢分析   │  予約する     │  ← 上段
 *  ├──────────────┼──────────────┼──────────────┤
 *  │ スタッフ相談 │  料金案内    │  アクセス     │  ← 下段
 *  └──────────────┴──────────────┴──────────────┘
 *
 * 画像サイズ: 2500 × 1686 px（LINE推奨サイズ）
 * 各エリア:   幅833px × 高843px（6分割）
 *
 * 使用方法:
 *   scripts/createRichMenu.js を一度だけ実行してリッチメニューを登録する。
 */

// リッチメニューの定義オブジェクト（LINE API に渡す形式）
const RICH_MENU_BODY = {
  // メニューの表示サイズ（large: 2500x1686 / compact: 2500x843）
  size: {
    width:  2500,
    height: 1686,
  },

  // LINEチャット画面を開いたとき、メニューを最初から開いた状態にするか
  selected: true,

  // メニューバーに表示するテキスト（タップでメニュー開閉）
  name:        'NAORU整体 渋谷院メニュー',
  chatBarText: 'メニューを開く',

  // 各ボタンのエリア定義
  areas: [
    // ---- 上段左: AI問診 ------------------------------------------------
    {
      bounds: { x: 0,    y: 0, width: 833, height: 843 },
      action: {
        // postback: チャット画面にテキストを表示せず、サーバーにだけ通知
        type:        'postback',
        data:        'action=start_inquiry',
        displayText: 'AI問診を始める',
      },
    },

    // ---- 上段中: AI姿勢分析 -------------------------------------------
    {
      bounds: { x: 833,  y: 0, width: 834, height: 843 },
      action: {
        type: 'message',
        text: 'AI姿勢分析を希望します',
      },
    },

    // ---- 上段右: 予約する ----------------------------------------------
    {
      bounds: { x: 1667, y: 0, width: 833, height: 843 },
      action: {
        // uri: タップするとブラウザで予約ページを開く
        type: 'uri',
        uri:  'https://reserve.naoru.info/',
      },
    },

    // ---- 下段左: スタッフ相談 -----------------------------------------
    {
      bounds: { x: 0,    y: 843, width: 833, height: 843 },
      action: {
        type: 'message',
        text: 'スタッフに相談したい',
      },
    },

    // ---- 下段中: 料金案内 ---------------------------------------------
    {
      bounds: { x: 833,  y: 843, width: 834, height: 843 },
      action: {
        type: 'message',
        text: '料金を教えてください',
      },
    },

    // ---- 下段右: アクセス ----------------------------------------------
    {
      bounds: { x: 1667, y: 843, width: 833, height: 843 },
      action: {
        type: 'message',
        text: 'アクセスを教えてください',
      },
    },
  ],
};

module.exports = { RICH_MENU_BODY };
